# Game

游戏自有运行时代码、编辑器工具、场景、设置和内容放在此目录：

- `Runtime/`：进入玩家构建的游戏代码；
- `Editor/`：场景生成、构建和其他仅编辑器工具；
- `Content/`：自有材质、模型、预制体、音频和配置；
- `Scenes/`：可运行场景；
- `Settings/`：URP 等项目资产。

第三方资源不得直接混入上述目录。M0 只建立边界和验证场景，M1 才开始加入玩法模块。
