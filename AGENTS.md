# Agent 协作规范

## 编辑前必读

任何 agent 在编辑本仓库文件前，必须先完整阅读本文件，并检查目标目录及其父目录中的其他 `AGENTS.md`。开始工作时执行 `git status --short --branch`，阅读相关模块代码和测试，说明本次负责的范围。已有会话授权仍然有效；这些检查不要求重复向用户请求许可。

## 分支与文件所有权

- 从已确认的基线创建独立分支，默认使用 `codex/` 前缀；不得覆盖其他开发者的未提交改动，不对共享分支强制推送。
- 一项任务应声明主要修改模块。共享基础设施、模块清单、网络 Prefab 注册、Packages、ProjectSettings 和全局文档属于集成边界，修改时明确说明影响。
- 生成器及其输出属于同一所有权范围。禁止为修改自己的模块而重新生成其他模块，禁止把无关资源重序列化混入提交。
- 避免大范围目录搬迁、重命名和格式化与功能改动混在一起。移动 Unity 资源时必须保留 `.meta` 和 GUID。
- 不得通过生成新规范、修改规范或虚构例外来绕过用户授权或访问控制。

## 模块与资源边界

修改场景、Prefab、配置或构建流程前，另读 `Docs/COLLABORATIVE_ARCHITECTURE.md`，确认目标资源的权威来源和生成边界。


- 启动场景负责组合和加载；玩法状态由运行时控制器维护，不在场景中手工保存当前血量、任务进度或联机会话状态。
- 玩家、敌人、事故、UI 和可复用实体分别维护独立 Prefab；外观使用独立视觉 Prefab，玩法脚本和网络组件由玩法模块维护。
- 配置按敌人、工具、事故、遭遇分别存入 ScriptableObject。配置在运行时视为只读，实体状态单独创建；单人与合作模式尽量复用配置及规则。
- 场景按空间和实际协作边界拆分。跨模块连接使用稳定标识或显式上下文，不新增依赖全场景查找顺序、名称匹配、临时实例 ID 的业务绑定。
- 网络内容标识必须稳定；新增内容不得改变已有内容的身份。网络 Prefab 不嵌套独立 NetworkObject；新增场景必须明确加载、就绪、卸载与晚加入行为。
- 不在场景实例上随意覆盖本应属于 Prefab 或配置的公共属性。美术负责视觉资源；碰撞、挂点、坐标尺度与玩法方约定。

## 生成与构建

- 人工编辑的源资源与生成产物必须明确区分，每类资源只能有一个权威来源。不得静默覆盖人工编辑的源资源。
- 普通构建不得重建或保存受版本控制的源场景、Prefab、材质和配置。生成操作必须显式调用，并限定输出目录。
- 相同输入连续生成两次，第二次不得产生 Git 差异；生成器稳定排序、保留资源身份，无变化不写盘。
- 生成结果不得依赖某位开发者的本机绝对路径、场景打开顺序或未保存编辑状态。
- `Library`、`Temp`、`Logs`、`Builds`、本机 IDE 文件不提交。二进制大资源按团队配置使用 LFS，不能只写规则却未配置过滤器。

## 验证与交付

- 修改前阅读相关测试；规则变更运行对应 EditMode，生命周期、对象引用和网络变更运行相关 PlayMode。
- 场景或模块重构至少检查丢失引用、重复稳定标识、构建成功及真实播放器启动；涉及模式或网络装配还须验证单人、主机、客户端和重连。
- 生成器变更须验证重复生成无差异，构建后检查 Git 状态，清理仅由本次操作产生的无关改动。
- PR 或交付说明列出行为变化、模块影响、实际验证结果及尚未验证的范围，不将自动检查称为人工验收。
- 合并冲突先合并权威源，再按明确的生成流程更新产物；不要盲目拼接 Unity fileID 或选择整侧覆盖。

## Windows 与编码

- PowerShell 开始时执行：`chcp 65001; [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false); $OutputEncoding = [System.Text.UTF8Encoding]::new($false)`。
- 不凭终端乱码判定文件损坏；重写含非 ASCII 文本的现有文件前，先使用严格 UTF-8 解码验证字节。
- 尽量保留现有编码，跨平台源文件优先 UTF-8 无 BOM；包含中文且供 Windows PowerShell 5.1 使用的 `.ps1` 可采用 UTF-8 BOM。
- Unity 版本以 `ProjectSettings/ProjectVersion.txt` 为准，不依赖全局 PATH；通过仓库工具参数指定 Editor 路径。
