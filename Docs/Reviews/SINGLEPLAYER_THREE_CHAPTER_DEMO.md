# 三章单人 Demo 评审

## 当前状态

- 分支：`feature/singleplayer-extended-incident`；
- Unity：`6000.0.82f1`；
- 场景：`Assets/Game/Scenes/SinglePlayer_ThreeChapterDemo.unity`；
- Windows Development Build：`Builds/SinglePlayerDemo-Windows/FunGame-SinglePlayerDemo.exe`；
- EditMode：56/56 通过；
- PlayMode：32/32 通过；
- Windows 构建及真实播放器检查点：通过；
- 1600×900 玩法画面自动截图：通过；第一章无旧路线碎片和未来章节悬浮装置，配电舱门、任务操作句和压力表距离标记清晰可见；
- 960×900 设置页截图：通过；选项区独立滚动，恢复默认、应用并保存和返回按钮固定在面板底端；
- 正式 Windows 构建贯通：通过；2/2 冷却、5/5 继电器、5/5 波次、最终结算与 325 均由正式场景运行时对象完成，详见 `SINGLEPLAYER_BUILD_PLAYTHROUGH.md`；
- 首次玩家时长、章节节奏、美术可读性和趣味性：待人工评审。

## 体验目标

本候选把单个冷却事故扩展为约 25–35 分钟的三章单人流程。时长来自诊断、工具切换、空间路线、并行事件和怪物组合变化，不使用空等计时或加长同一进度条。自动化只能证明流程门禁可完成，不能证明真实游玩已达到目标时长。

## 三章结构

三章现在位于首尾相连的三个功能舱室。第一章开始时配电舱封闭；冷却恢复后舱门开启，HUD 将冷却标记为在线。配电恢复后风暴核心舱开启，HUD 同时保留冷却与配电在线状态。最终校准完成后，三个系统全部在线。

### 第一章：冷却舱事故

第一章需要连续稳定两条冷却支路。第一条用于学习完整语义，完成后事故会切换到另一套合法工位布局；第二条继续使用相同规则，但泄漏、维修组件和替换管件的位置发生变化。

每条支路均包含：

1. 分别检查压力表和冷却泵外壳；
2. 使用线路桥接器完成“检测端点—连接旁路—验证回路”；
3. 根据声音、喷雾和逐级提示定位泄漏；
4. 密封后处理一次短促防卫；
5. 松开损坏连接、搬运并安装替换管件、重新紧固；
6. 回到压力表验证压力，再从控制台复位。

事故在三组合法工位布局间随重置轮换，本流程会至少实际使用其中两组。提示先描述系统现象，停留较久后才给出明确动作。

### 第二章：风暴继电器

- 位于冷却舱后方的独立配电舱，第一章结束前设备与怪物均不显示；
- 五处继电器分布在舱内两侧与前后区域，每处复用线路桥接器完成三步稳定；
- 舱内提供线路桥接器和冲击扳手工具站，不需要返回冷却舱换工具；
- 同时出现直行、侧袭、远距脉冲和重甲干扰体；
- 必须同时完成五处继电器和设备防卫才能进入下一章；
- 设备离线时可从风暴控制台重启本章。

### 第三章：核心校准防卫

- 位于配电舱后方的独立风暴核心舱，仅在冷却与配电均恢复后开放；
- 五轮敌人组合依次突出侧袭、远距脉冲、混合突破、重甲护送和最终合流；
- 每轮结束后必须回到控制台写入一次校准，才会开始下一轮；
- 五轮共享同一个风暴核心，避免重叠目标和额外规则；
- 最终提交后显示完整演示结算。

## 怪物变式

所有怪物沿用“接近设备—明显蓄力—结算设备损伤—扳手击退”的统一规则：

- 直行干扰体：中等速度与耐久，直接施压；
- 侧袭附着体：低耐久、高速度，从设备侧面接近；
- 远距脉冲体：在更远距离攻击，蓄力更长，使用蓝色轮廓；
- 重甲干扰体：高耐久、低速度、长蓄力与低击退，使用厚重装甲轮廓。

变式只改变路线、速度、耐久、距离、击退和形状，不增加元素克制、枪械或复杂行为树。

## 325 隐藏内容

场景中藏有一块可擦拭的旧维修铭牌。发现后会显示维修队编号 `325` 和一条不会影响主线数值的记录；结算会记录是否发现。评审时不应提前告诉首次玩家具体位置。

## 操作

- `WASD`：移动；鼠标：观察；`Shift`：冲刺；`Space`：跳跃；
- `E`：交互、检查、拾取、安装和控制台确认；
- 鼠标左键：使用当前工具；
- `Q`：丢下手持任务物；
- `Esc`：暂停菜单。

## 游戏内引导

- 屏幕左下角持续显示当前唯一主动作，直接写明目标外观、所需工具和输入键；
- 橙色菱形标记当前主目标并显示距离，目标在身后或画面外时会停留在屏幕边缘；
- 后方目标使用左右死区和上一侧记忆，轻微转动视角时不会在屏幕两边来回跳；
- 战斗与维修并行时，紫色标记额外指出最近威胁，不覆盖主目标；
- 随机工位布局、拾取状态、装备工具、章节失败和波次校准都会即时重算目标，不依赖固定坐标；
- 联锁桥接、泄漏密封和单座继电器显示当前进度；40 秒无推进时补充输入排障提示；
- 通关后只提示存在旧维修铭牌，不显示具体藏匿位置，保留 `325` 的探索感。

## 关键物品美术识别

本轮已为压力表指针与表盘、泵检查屏与状态灯、冲击扳手、线路桥接器、密封枪、三相联锁箱、发光泄漏口、带螺栓法兰、双环替换管、继电器线圈、五段校准台和防卫核心补齐程序化低模轮廓。原交互根对象仍保留碰撞与状态，美术子物体不承载玩法逻辑。

针对“模型看得到但准星没有交互”的反馈，泵体外壳现在会把 `E` 交互转发到检查面板，且不再以大型方盒遮住面板左侧和机械法兰；机械法兰、压力表、联锁、泄漏口、替换管、三类工具架、继电器和校准台的根碰撞体均覆盖完整可见轮廓。怪物侧翼、脉冲冠和重甲护板保留随父怪物处理的碰撞体，瞄准这些突出部分也能命中怪物。场景测试会在每次生成后检查上述范围，避免后续改模型时再次脱节。

## 人工评审清单

- [ ] 不读本文也能找到压力表与泵体，理解先诊断再维修；
- [ ] 全程只依靠左下角任务句与目标标记，无需开发者口头告知下一步；
- [ ] 标记在目标位于身后、画面外或随机工位布局切换后仍指向正确对象；
- [ ] 完成冷却后配电舱门开启，完成配电后风暴核心舱门开启，过程中能感受到舰船能力累计恢复；
- [ ] 舱内没有无法解释的地面碎片、悬浮零件或与可见模型明显不一致的空气墙；
- [ ] 从左、中、右侧瞄准泵检查屏均显示交互提示，瞄准泵体外壳也能执行同一检查；
- [ ] 准星对准机械法兰正面螺栓、泄漏环、继电器突出触点、校准台屏幕和怪物外伸装甲时均可执行预期动作；
- [ ] 原先报告的中段卡点不再出现；若复现，记录当时章节、左下角操作句和进度数字；
- [ ] 三种工具能从轮廓、颜色、动作词和目标类型区分；
- [ ] 第一章不会因错误工具、任务物掉落或事故重置永久卡住；
- [ ] 第二章的五个继电器与防卫形成同时压力，但目标仍可理解；
- [ ] 四类怪物的路线和轮廓差异可辨认，规则仍然容易记忆；
- [ ] 第三章五波之间的组合升级可辨认，每波结束后能自然理解需要回控制台校准；
- [ ] 首次完整流程落在约 25–35 分钟；若明显偏短，优先增加路线与事件组合，不堆生命值；
- [ ] 低模设备、章节辅助色、BGM 和命中反馈形成统一表现；
- [ ] 找到或错过 325 铭牌都不阻塞通关；
- [ ] Console 没有项目代码异常或持续错误。

## 自动验证入口

```powershell
.\Tools\Run-UnityTests.ps1 -Mode All -UnityEditorPath 'D:\Unity\Hub\Editor\6000.0.82f1\Editor\Unity.exe'
.\Tools\Build-SinglePlayerDemo.ps1
.\Tools\Test-SinglePlayerDemoCompletion.ps1
.\Tools\Test-SinglePlayerDemoBuild.ps1
```

开发构建还支持 `--demo-capture-main-menu`、`--demo-capture-gameplay` 与 `--demo-capture-settings` 三个专用画面检查参数；截图会写入可执行文件所在目录，普通游玩不会产生截图。

## 人工计时入口

```powershell
.\Tools\Run-SinglePlayerDemoPlaytest.ps1
```

脚本会打开真实玩家构建。完成结算后从菜单退出，脚本会读取结算记录中的游戏内用时，输出到 `TestResults/SinglePlayerDemo-ManualTiming.txt`。`PASS` 表示完整通关且用时处于 25–35 分钟；`OUTSIDE_TARGET` 表示已经通关但需要继续调节内容节奏；`NOT_COMPLETED` 表示本轮没有到达结算。

## 已知边界

- 约半小时目标仍需首次玩家实测，当前不得声称已经通过时长门禁；
- 三章使用同一飞船内首尾相连的三个功能舱室，仍属于程序化低模原型，不代表最终关卡美术完成；
- 角色、敌人和环境使用程序化低模组合，尚无正式动画；
- 后续联网同步不在本候选范围内。
